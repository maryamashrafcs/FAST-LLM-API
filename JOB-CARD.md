# Job Card: Task Prioritizer & Categorizer

## Purpose
Classifies incoming task descriptions into a fixed category, assigns a priority level, and generates a one-sentence summary for the To-Do app.

## Input Schema
```json
{
  "text": "string (1-2000 characters)"
}